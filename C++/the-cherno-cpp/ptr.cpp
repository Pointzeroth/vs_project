#include <iostream>

#define LOG(x) std::cout << x << std::endl

int main()
{
    int var = 8;
    void* ptr = nullptr;       //定义了一个无类型的指针，它的内存地址是0    NULL/nullptr，不能直接赋值0，0地址是无意义的。
    std::cin.get();
}